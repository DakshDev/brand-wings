
      <section className="bg-lime-50 py-20 px-4">
        <h2 className="text-stone-500 text-3xl md:text-5xl text-center font-bold">Clients we worked with</h2>
        <div className="flex flex-col md:flex-row items-center gap-8 md:gap-8 mt-12 md:justify-center">
          {[1, 2, 4, 5].map((num) => (
            <img
              key={num}
              src={`/images/client-logo-${num}.png`}
              alt={`logo-${num}`}
              className="object-contain"
            />
          ))}
        </div>
      </section>
